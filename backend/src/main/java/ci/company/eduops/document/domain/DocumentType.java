package ci.company.eduops.document.domain;

/** Types backed by the PostgreSQL {@code document_type} enum. */
public enum DocumentType {
    STUDENT_FILE,
    SCHOOL_CERTIFICATE,
    ENROLLMENT_ATTESTATION,
    REPORT_CARD,
    TRANSCRIPT,
    RECEIPT,
    INVOICE,
    FINANCIAL_STATEMENT,
    SUMMONS,
    STUDENT_CARD,
    OTHER
}

